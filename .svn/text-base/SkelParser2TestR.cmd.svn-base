REM execute the field offsets analysis REXX. Assumes that the XML configuration file name will be passed 
REM as the first parameter, that the filename is given without the .XML extension and that the 
REM file is in the parent directory above this one.
@echo off
parserDB2.rex "..\%1.xml"

